# LEARNINGS.md — index

GENERATED by `tools/gen-learnings-index.py`. Do not hand-edit; re-run it.

`LEARNINGS.md` is ~800 KB and nobody reads it whole. Scan this index, then jump:

```bash
grep -n "<heading text>" LEARNINGS.md   # then Read with that offset
```

Every heading states its lesson in full, so **the index is the summary** — if a
line does not sound relevant, neither is the section under it. `L<n>` is the line
in LEARNINGS.md at generation time; re-run the tool if the numbers look off.

Roughly half the file is transferable method (the topic groups) and half is
per-backend porting detail (Per-model port notes + the family cross-reference).
Reach for the topic groups when you are stuck on a *technique*, and the
cross-reference when you already know which model you are touching.

## Index by topic (279 lessons)

**Security & untrusted input** (2)

- L1444 — Two more untrusted-input lessons: an HTTP server reads the body before your auth check, and a parser that abort()s on malformed input is a DoS a fuzzer finds in seconds
- L1471 — Every hand-rolled file parser sizes a buffer from an untrusted length field — that is the #1 memory-safety bug class, and a multi-agent audit finds them fast

**Compliance, provenance & marking (EU AI Act)** (7)

- L187 — A watermarked TTS output cannot be A/B'd with `cmp` — and whisper LID is not an accent metric
- L747 — Moving a release tag is CLEAN when the fix touches only release TOOLING — the test is provenance, not "never move a tag"
- L10819 — Always rebuild the box-under-test before benchmarking
- L15908 — EU AI Act audit — provenance not suffix
- L15966 — EU AI Act — a PRESET voice can be a real person
- L16416 — consent record — BIND to the audio, don't chain the log
- L17711 — watermark detector — swap the STATISTIC, not the threshold

**Build, CI, release & packaging** (21)

- L91 — A packaging step that destroys the state a later step reads — and scripts that only run in a release job
- L395 — A guard job that runs ONE compiler family guards one compiler family
- L683 — A green release job is not a shipped artifact — verify what you DELIVER, not what the workflow says
- L812 — "Linked in CMake" is NOT evidence the code SHIPS — the linker drops an object nothing references
- L1032 — Path-filtered CI workflows ROT — the first re-triggering change inherits ALL the debt accumulated since the last run
- L1052 — Scoped `ctest -R` on a PARTIALLY BUILT tree has two name-collision traps: configure-time add_test and catch_discover NOT_BUILT placeholders
- L1337 — Porting a libllama-hosted reference onto our in-house Qwen3 was clean on first contact; and Kaggle `kernels_logs()` is the escape hatch when the output is page-capped
- L1395 — ggml disables CUDA graphs below Ampere (cc < 800) — an Ampere-only re-warm bug can't be reproduced on Kaggle's P100/T4
- L1694 — Two concurrent HuggingFace uploads from one machine cause spurious mid-batch failures — serialize them (#192 aligner re-ship)
- L2163 — Never benchmark on a near-full disk — SIGBUS + nondeterminism masquerade as a code signal (§192)
- L2239 — Kaggle: datasets are per-account, and the ccache dataset has a required shape (§213)
- L2661 — Kaggle regression kernels: write output incrementally, crash-guard everything
- L2788 — Regenerate Go cgo LDFLAGS from a *linux-equivalent* config, never macOS
- L2803 — cmake-js defaults its build dir to `build/` — point it elsewhere (§166)
- L2957 — HF Space is a separate, FLAT repo that silently drifts from `hf-space/`
- L4260 — Windows / MSVC portability
- L5498 — GitHub Actions workflow triggers — `master` → `main` rename gotcha
- L5644 — libcrispasr.a + libcommon.a both define stb_vorbis / miniaudio impl (Linux ld dies)
- L10883 — Kaggle as a batch-rebake target: seven fragilities the script has to work around
- L14327 — CI has no NVIDIA GPU — a CUDA-only graph path (mimo RVQ) is guarded by an on-Kaggle exact-parity smoke, not CI (#309)
- L15154 — #313 Rust crates on crates.io via git dep

**Multi-surface wiring — CLI / C ABI / bindings / server** (24)

- L316 — A gate that ANDs "both sides known" is only as alive as its weakest detector
- L555 — A new REQUIRED request field is a breaking API change on a schedule you don't control — deny the sub-feature, don't refuse the request
- L643 — A hardcoded decode cap silently ignores --max-new-tokens — and forwarding it naively SHRINKS a backend
- L720 — A capability flag is a PROMISE the backend must keep — an unimplemented cap disables the safety nets keyed off it
- L1014 — Two writers on one output field at different pipeline stages = an ordering bug — fix the ORDER, don't bolt on provenance
- L1073 — A binding wrapper that has never been EXECUTED is untested code — and parallel agents produce cross-cutting conflicts only the verifier can see
- L1663 — A persistent KV cache reused from a larger allocation makes local `max_ctx` the wrong stride (#171 VibeVoice server leak)
- L2570 — Backend-name guards must match the *registered* name, by prefix when aliased (#171, #174)
- L2704 — A feature has ~8 front-ends — wiring it into one isn't "done" (§166)
- L2730 — A dry-run "preview" must mirror the real resolver, or it lies (§166)
- L2815 — A WebSocket server that "works in the browser" can still be RFC-broken (§166)
- L2829 — Server warmup is the launch-time differentiator vs the CLI (#165)
- L2842 — Don't throw on the per-request server path; surface the real error (#165)
- L6169 — Chatterbox CAMPPlus phase 1 — Kaldi fbank front-end
- L8711 — voxcpm2 VAE — Python wrapper that captures kwargs without forwarding
- L13247 — 2026-07-02 — `--gpu-backend` was silently ignored (#214)
- L13935 — Before starting a "top-priority, profiled" perf target: check whether it's already been tried-and-rejected, and whether a parallel session owns it
- L14416 — A public API with no caller is a harness-blind zone announcing itself — and an ONNX export may FOLD a BatchNorm your C++ still expects as tensors
- L15088 — #311 --strict-pipeline SHIPPED
- L15231 — #315 vibevoice long-form ASR budget: PR fixed only session; CLI+server needed the adapter…
- L15267 — #324 server diarize DROPPED 59% of the transcript
- L17417 — Parakeet #208 long-audio + enc-cache collapse
- L17524 — session-state fixes + moonshine CPU-only SOLVED
- L17748 — Wyoming TTS marked nothing — FIXED 8f4197ec

**Parity, the diff harness & ground truth** (38)

- L20 — A guard has to assert the property the CONSUMER reads — segment spans are not cue times
- L360 — The diff harness starts where its INPUT starts — check what it is fed before trusting parity
- L587 — When a clone/zero-shot path misbehaves but the BAKED/preset path is clean, diff the two paths' DATA byte-for-byte — the difference IS the bug
- L850 — Cosine, correlation and peak-match are ALL scale-invariant — a uniform gain error passes every one of them
- L918 — A logits-level diff harness at cos 1.000000 says nothing about the TABLE that turns logits into labels
- L993 — When you can't run the acceptance roundtrip, ship the path gated default-OFF + a provable-equivalence argument — don't block on the model
- L1090 — Deterministic DROPOUT of whole words/tokens (not garble) = a zeroed weight BLOCK in the shipped GGUF — scan converted tensors for zero-norm rows BEFORE debugging the port
- L1135 — A TTS AR model that "never stops" is often a WRONG-SAMPLING-PARAMS bug, not a stop-head/port bug — read the model card's generation params
- L1430 — Validating a TTS perf change by WAV correlation is invalid — melotts `--seed` isn't deterministic; use ASR round-trip or a deterministic-ASR bit-check
- L1512 — MelsTime mel layout IS ggml Conv1d input layout — transposing corrupts with cos ~0.3, not a gradual drift
- L1557 — Baked mel filterbank layout MUST match core_mel's `fb_layout` — a transpose produces cos_min ≈ −0.15, not a gradual drift
- L1760 — A production default that diverges from the upstream reference is a whole class of bug the stage-cosine diff can't see (#192 TADA)
- L1800 — A forced aligner is not an ASR model — its free CTC argmax is all-blank (#192)
- L1809 — When the engine and the PyTorch reference produce the SAME wrong output, the engine is right — the harness input (usually the prompt) is the bug (moss-transcribe)
- L1834 — An input feature the checkpoint wasn't trained with is worse than nothing — the moss-transcribe time-marker experiment (#218)
- L1859 — Stage the PyTorch reference load — encoder and LM as separate standalone modules, never the monolithic `*ForCausalLM` (moss-transcribe)
- L1879 — Parakeet single-pass DROPS whole sections of long audio — chunk-and-merge, and pick the reference clip carefully (#208b)
- L2175 — Always A/B against the *upstream reference*, not just your own option matrix (§216)
- L2226 — TTS WAV md5 is only a valid parity gate with a pinned seed
- L2647 — Regression transcripts need WER tolerance, not byte-exact match (#92)
- L3657 — Methodical debugging of ported models against ground truth
- L5764 — Chatterbox base T3 sampler parity — Gumbel-max → torch.multinomial
- L5803 — Chatterbox HiFT vocoder parity nits
- L8834 — Diff-harness "drift" is mostly the GGUF quant, not a code bug
- L12109 — Pocket TTS — 12 bugs from stub to cos=0.999997
- L13524 — ASR roundtrip does not validate audio ONSETS; reproduce with the original pipeline before debugging your runtime
- L13568 — Porting the missing sub-model is necessary but not sufficient — hunt the hardcoded "unconditional" defaults that ignore it
- L13641 — Python reference dumpers must apply identical audio conditioning as the C++ runtime
- L13658 — A `frames/token` (or chars/sec) length heuristic silently truncates diffusion TTS — run the duration predictor the model already ships
- L13763 — A faithful port can still loop: get the bf16 blueprint's behaviour on the SAME audio before hunting a runtime bug
- L13823 — Generation-config defaults are part of the blueprint contract — a masked-iterative TTS with the wrong sampler knobs degenerates to SILENCE, not to bad audio
- L13967 — CosyVoice3 CFM Euler steps: 6 is the sweet spot, and validate it with log-mel corr vs full-steps — NOT ASR roundtrip
- L14159 — Exact greedy code-parity is unachievable for a QUANTIZED AR audio LM — confirm the port with a step-0 logit-rank probe, not byte-exact codes
- L14209 — A GPU-vs-reference RTF gap that survives kernel parity is per-step HOST detours — fuse them into the step graph; unified-memory Metal hides exactly this class of cost
- L14302 — A normalization stage amplifies upstream float error — input-align it before trusting its cos (§248 MBR)
- L15440 — #333 madlad400 quants + T5 parity
- L16312 — chatterbox hift_pcm(ref_mel) cos≈0.879 was a diff-harness layout bug (source_stft fed transposed), not a…
- L17942 — Chatterbox Multilingual V3: checkpoint names, quant hashes, and a non-silent clone are not parity

**ggml graphs, allocation & caching** (43)

- L626 — Reusing a cached scheduler graph across `sched_reset`/`alloc` cycles is CPU-safe but SIGSEGVs on GPU — the reused input tensor is bound to the prior cycle's freed buffer
- L1196 — Metal-validated GPU TTS graph paths DID transfer cleanly to CUDA — the stricter-CUDA risk is real but not universal
- L1232 — BatchNorm-fold must respect the conv tensor's dtype — and a per-backend fix usually has siblings
- L1266 — "Migrate host KV to device-resident" is a ~1% win on a compute-bound transformer decoder — measure the fraction before the rewrite
- L1566 — Never cache a ggml compute graph across calls that share a scheduler with a larger graph — the allocator regrows and frees the cached graph's GPU buffers
- L1577 — Flow-matching DiT with JointAttention REQUIRES attention masking even for unconditional generation — skipping masked KV tokens silently corrupts output (2026-07)
- L1912 — A cached cgraph is NOT re-entrant with ggml_backend_sched — the 2nd reuse silently corrupts (#208)
- L1954 — On Metal a cached decode graph is dispatch-bound, not alloc-bound — measure the step's parts, don't trust wall time (§210, PR #207)
- L2111 — Precomputed attention masks in a GGUF are dead weight if the runtime rebuilds them (§192)
- L2279 — Cross-attention KV is a free F16 win — encoder-decoder backends (§176i)
- L2307 — Localizing a GPU miscompute with CPU-vs-Metal diffing — the ggml_backend_sched weight-less-first-op trap (§206)
- L2461 — A byte-identical standalone reproducer is the only way to (dis)prove a "ggml bug" (§203)
- L2491 — In-place device-KV decode graphs on Metal: three hazards (§176b+c, §203)
- L2599 — Never pass `ggml_graph_get_tensor` directly to `ggml_backend_tensor_set` (#164)
- L2619 — `rope_theta=0` means "no positional encoding" — skip RoPE entirely (#164)
- L2628 — `ggml_siglu` ≠ PyTorch `glu` — the gate/value halves are SWAPPED (#81)
- L2697 — Asymmetric rel-pos shift uses the same formula as symmetric (#81)
- L2742 — Graph input tensors that nothing consumes are invisible to `ggml_graph_get_tensor` (#164)
- L2775 — `ggml_backend_cpu_set_n_threads` asserts CPU — guard it after `init_best()`
- L2888 — Decompose ConvTranspose1d → mul_mat + col2im_1d; port col2im per-backend (#155)
- L2928 — Beam-search KV snapshots must stay on-device (#161)
- L3211 — ggml / inference engine
- L3783 — ggml graph allocation: gallocr vs compute_with_ctx
- L5131 — 2026-04-22 - No-gpu mode must gate `ggml_backend_load_all()`
- L7236 — 2026-05-05 — ggml fork patches we carry (must re-apply on every ggml bump)
- L8507 — voxcpm2 perf — per-step ggml graphs, Metal, SIMD layouts
- L8791 — ggml broadcast hides size-mismatch bugs
- L11011 — ggml scheduler tightened cross-backend tensor resolution between §56 and 2026-05-26 (PLAN #115)
- L12265 — Pocket TTS — manual CPU → ggml compute graph rewrite
- L12504 — AudioSeal ggml port — ggml_pad_ext convention trap
- L12581 — GPU weight mirrors for mixed legacy / graph codebases
- L12705 — ggml_graph_get_tensor hash invalidated by ggml_gallocr
- L12898 — read_tensor_f32 weight pre-cache for VITS-family TTS
- L12911 — Single-token embed graph elimination for LLM-ASR
- L13425 — A once-allocated-then-reused bucket step graph is a CUDA-graph-capture hazard: reset+alloc the sched EVERY step
- L13484 — A cached cgraph is invalidated by ANY other graph on its scheduler — reset+alloc per step is NOT enough
- L13584 — TTS code predictor: the dispatch overhead is real, but "fuse into one graph" and "skip the reset" are BOTH wrong — the fix is a sched-free persistent graph
- L13653 — Any T-sized `ggml_view` into a fixed-length weight table must be bounded by the tensor's real length — long inputs overflow it and abort
- L13795 — gallocr does NOT protect input-flagged tensors across computes — re-set EVERY input before EVERY compute of a persistent graph
- L13856 — Vocoder/codec conv graphs hide three graph-construction wastes that dwarf the actual conv FLOPs — profile per-node before assuming it's inherent
- L13903 — Before optimizing a graph's *dispatch*, split host-encode vs GPU-execute — and never trust a GPU benchmark taken under load
- L13998 — A "GPU" model can be silently running on CPU — audit for hot graphs pinned to backend_cpu and CLI adapters that never forward use_gpu
- L17486 — PR #244 dequant-cache fix MERGED

**GPU portability — Metal / CUDA / Vulkan** (24)

- L1171 — When the full system needs an unavailable resource (model / GPU), factor the risky logic into a pure helper and prove IT on synthetic data
- L1286 — Verify a roadmap "broken/OPEN" claim empirically before implementing it — the codebase may have outgrown the note
- L1366 — A Metal masked-attention-padding penalty does NOT transfer to CUDA, and a bucket-width change is byte-identical on Metal/CPU but not on CUDA
- L1413 — ggml-metal im2col starves batch-1 convs — 3–11 threads/threadgroup at N=1, ~40× below bandwidth
- L2075 — A "garbled GPU output" bug can live entirely downstream — A/B the GPUs before localizing (§192)
- L2129 — MoltenVK `mul_mm`/`mul_mat_vec` downconvert src0 to f16 regardless of stored dtype (§192)
- L2140 — Vulkan has no `REPEAT f16→f16` — cast K/V to F32 *before* the GQA repeat (§192)
- L2153 — On macOS, GGML_METAL is default-ON and silently wins over Vulkan (§192)
- L2203 — §214 Metal batched (B=2) quantized mat-vec ≠ the single-token PREC_F32 path
- L2414 — Metal's q8_0 mat-vec kernel requantizes activations to q8 and ignores the F32 prec hint (§205)
- L2912 — Measuring GPU perf on a thermally-throttling laptop GPU
- L9157 — WDDM idle-clock-state hysteresis on consumer/laptop NVIDIA SKUs
- L9294 — Chatterbox #83 Round 9 — S3Gen UNet GPU drift on Metal
- L10547 — FA per-head additive mask CUDA kernel — what the upstream signature already gave us
- L11165 — funasr CUDA !-loop — all-NaN prefill logits (issue #125, §136)
- L12475 — Cohere flash-attn crossover
- L12535 — conv_transpose_1d GPU TDR — naive loop is O(IL), not O(K/s0)
- L12737 — flash_attn_ext vs scalar attention — numerical divergence
- L12848 — VibeVoice TTS garbles only on AMD RDNA4: coopmat2 flash-attention
- L14033 — Micro-optimizing an already-BLAS'd path with a GPU-ggml port is usually a dud — check for existing Accelerate/cblas and measure encode-vs-execute BEFORE porting
- L14356 — "Backend miscomputes my pipeline" ≠ "op X is broken": arbitrate with test-backend-ops, and beware aggregate precision (#304 native-Vulkan post-mortem)
- L16274 — Chatterbox GPU UNet performance fix
- L16513 — cosyvoice3 \"CUDA test FAIL\" was a registry-alias + flow-quant discovery bug, NOT a CUDA bug
- L17804 — "GPU picks a different token than CPU" is usually NOT a miscompute in an AR audio model — dump the LOGITS, and don't reach for the repetition detector

**Quantization** (10)

- L1630 — Auditing CrispASR against CrispEmbed's bug classes: a weight reader with no quantized branch fails silently (2026-07)
- L1710 — Quantizing a forced-aligner: q8 everywhere (incl. lm_head) is bit-identical; q4 on the encoder is not (#192 TADA aligner)
- L2760 — Orpheus/SNAC TTS: `token_embd` must stay F16 for sub-Q8 quants
- L3362 — Quantisation and memory
- L3637 — Quantization
- L12466 — F16 precision loss in deep WaveNet stacks
- L12483 — MeloTTS + BERT quantization — what works and what doesn't
- L13577 — Re-quantized GGUFs on HF must be re-baked when the quantizer adds carve-outs
- L13665 — Sub-8-bit quantization of an audio tower fails as BEHAVIORAL collapse (loops, empty output) — and there is no per-block cliff to bisect
- L17301 — imatrix quant SHIPPED

**Perf measurement & A/B discipline** (8)

- L1304 — Per-step matvec dispatch overhead is LOAD-DEPENDENT — a "win" measured on a busy box is mostly a contention artifact
- L1774 — Best-of-N only helps if the scorer measures what you care about — TADA's reconstruction scorer is blind to duration outliers (#192)
- L3398 — CPU vs ONNX vs PyTorch baselines
- L3841 — Performance: what faster-whisper / insanely-fast-whisper do
- L5138 — 2026-04-23 - FireRed decoder optimization triage
- L10846 — Distinguishing "slow run" from "hung run" — CPU time ≪ wall time is the signal
- L10981 — Cross-backend bug-sweep methodology — pair the cap survey with an empirical A/B
- L12919 — §176 runtime optimization audit methodology

**Long-form audio, VAD & chunking** (8)

- L1535 — LLM-ASR (SALM) instruction-echo on short windows is genuine model behaviour, not a port bug — gate the input, don't chase the prompt
- L1588 — The Mimi codec transformer must be causal — non-causal silently truncates long audio; the >250-frame WER A/B settled it (2026-07)
- L2981 — VAD + chunking
- L3914 — VAD integration and long audio
- L10670 — TDT single-pass over a full long utterance is numerically fragile to codec-level audio noise
- L14778 — #227 "--vad-import wants ggml-tiny" = LID not VAD
- L15000 — #300 streaming diarization SHIPPED
- L15596 — #89 parakeet-ja long-form FIXED

**Tokenizers, prompts, language & text** (17)

- L447 — MOSS-TTS-Local 4B stop runaway was a PROMPT-TOKENIZATION bug — non-compositional BPE, not the forward (#249, 2026-07)
- L497 — Cross-lingual TTS needs the target language plumbed through /v1/audio/speech (#249/#304, 2026-07)
- L1786 — A converter that embeds a tokenizer MUST embed the merges — a silent `try/except: pass` byte-fallback breaks everything downstream (#192)
- L3484 — Language handling
- L5305 — FireRedPunc / fullstop-punc — BERT punctuation restoration
- L6310 — Chatterbox 24 kHz prompt mel — module 4 phase 3
- L7469 — Text LID via fastText — GlotLID-V3 + LID-176
- L7680 — Text LID via CLD3 — Google compact language detector
- L12770 — VibeVoice TTS: missing BPE merges still need BPE behavior
- L13300 — 2026-07 — SentencePiece tokenizer taxonomy: greedy longest-match is wrong for BOTH Unigram and BPE
- L13557 — SentencePiece `byte_fallback` is not optional decoration — OOV emoji/symbols must become `<0xHH>` byte tokens, and a `utf8_aligned` Viterbi dead-ends at a multi-byte lead without it
- L13706 — The prompt contract is part of the port: a specials-only tokenizer silently sent EVERY glm-asr prompt instruction-less
- L13741 — Unigram run-length loop metrics pass 2-gram cycles — and a "raw" baseline is only raw if the disable gate actually exists
- L14914 — #249 MOSS-Local 4B stop-runaway = PROMPT tokenization bug
- L15293 — #329 TTS target language
- L16367 — cohere language whitelist + probe LID
- L16485 — CV3 phase 6 speech_tokenizer_v3 ggml port

**Mel, codecs & the audio front-end** (15)

- L1524 — A one-frame error in a codec ENCODER shifts every RoPE position and shows up as an onset artifact, not a gradual drift — and a padding scheme faithful to one codec can be wrong for another
- L2388 — A glibc-only crash reproduces on macOS under AddressSanitizer — and the bug is often a non-power-of-two FFT (§205)
- L2679 — Streaming conv modules need cached left context, not zero-padding (#81)
- L2857 — FFT size must match upstream exactly (Mini-Omni2 / Whisper mel)
- L3166 — mel / preprocessor
- L3314 — Mel spectrograms
- L3452 — Audio format lessons
- L4423 — Kyutai STT: causal padding, interleaved RoPE, and codec-based ASR
- L4557 — FireRedVAD: FSMN Conv1d replication
- L6358 — Chatterbox atomic native voice clone — the resampler + 5-cond install
- L11335 — moshi / Mimi RVQ codebooks: decode uses embed_sum / cluster_usage
- L12358 — MeloTTS (VITS2) — from zero to BERT conditioning
- L14875 — #245/§232 qwen3-tts CP_DIRECT + codec FASTCONV DONE
- L17200 — glint MP3+AAC TTS output §225 SHIPPED
- L17683 — voxcpm2 CausalTransposeConv1d kwargs gotcha

**Process, triage & documentation discipline** (18)

- L419 — When instrumenting the suspect file produces NO output, you are editing the wrong file — look for a second copy
- L512 — "The model emits it inline as text" is a claim to VERIFY, not to document — structured data you never parsed looks identical to a model limitation
- L769 — A positional arg landing on the WRONG parameter is invisible in review and baked into the weights
- L974 — PLAN "OPEN" items are frequently already shipped — audit against the CODE, never the prose
- L1212 — Audit a whole roadmap CLUSTER in one measure-first pass before implementing any of it
- L1518 — Handover docs can be dangerously stale — read the actual Python source, not the handover
- L2267 — DRY shared headers can exist for months before callers migrate (§175)
- L3560 — Regression testing discipline
- L3587 — Specific bugs that cost us a day each
- L4050 — CLI ↔ library DRY refactor
- L5614 — Audit script ≠ behavior test
- L10755 — Long-form ASR has three distinct failure classes, not one (2026-05-25, generalising issue #89)
- L12681 — beam_size default — greedy vs beam-5
- L12805 — VibeVoice TTS start clicks: distinguish decoder PCM from CLI post-processing
- L13353 — Issue #89 close-out: four transferable lessons
- L13648 — Issue triage discipline: check the codebase before leaving issues open
- L14275 — `git apply --3way` STAGES its result — a later `git add X && git commit` sweeps it up
- L16553 — CrispEmbed #31 WASM OCR e2e

**Per-model port notes** (44)

- L267 — A model's capability list cannot be inferred from its vocabulary — and forcing a fake list does not simulate having the capability
- L2872 — Multi-stream token architecture (Mini-Omni2)
- L3522 — Model architecture comparisons
- L4466 — FireRedASR: Conformer encoder debugging
- L5419 — TTS / Vocoder (Chatterbox HiFTGenerator)
- L5681 — Chatterbox-Turbo conformer encoder — ggml layout traps
- L5732 — Chatterbox repaired GGUF split — stage-specific regen
- L5907 — Chatterbox voice cloning — bake to GGUF, load via `--voice`
- L5960 — Chatterbox VoiceEncoder native port — module 2 of voice cloning
- L6066 — Chatterbox S3Tokenizer V2 native port — module 3 of voice cloning
- L6235 — Chatterbox CAMPPlus phase 2 — TDNN forward
- L7131 — T5-family translation runtime traps (May 2026, MADLAD-400 debugging)
- L7837 — IndexTTS-1.5 TTS backend
- L8304 — Speaker verification — TitaNet
- L8352 — Parakeet-TDT greedy decode — blank + duration=0
- L8418 — VibeVoice 1.5B TTS voice cloning: acoustic + semantic dual encoder
- L8901 — SANM-encoder family (FunASR / SenseVoice / CosyVoice)
- L11294 — Round 10 — SpeechT5 + Dia TTS backend ports (2026-05-31/06-01)
- L11369 — Dia 1.6B TTS — what ACTUALLY fixed it
- L11423 — FastPitch TTS — non-autoregressive parallel TTS port (§133, 2026-06-02/03)
- L11589 — SpeechT5 TTS decoder — what ACTUALLY fixed it
- L11663 — Parler TTS — T5 + MusicGen decoder + DAC 44 kHz
- L11795 — MAES beam search for TDT transducers
- L11897 — Bark TTS — what ACTUALLY fixed it
- L12444 — OpenVoice2 voice cloning — ggml data layout
- L13182 — TADA encoder port: staged model loading on constrained RAM (§221)
- L14496 — #195 parakeet-ctc-1.1b-ja CTC routing
- L14539 — #205 --max-len text-split + granite-plus
- L14656 — #221 irodori voice cloning SHIPPED
- L14819 — #231 cohere-arabic loop = corrupt GGUF
- L14932 — #249 MOSS voice cloning SHIPPED
- L14980 — vibevoice #299 runtime-reproduced
- L15045 — #308 whisper subtitles double-caps
- L15526 — #334 cosyvoice3 clone bugs
- L15664 — #93 voxtral-tts SHIPPED
- L16144 — ARK-ASR-3B port SHIPPED
- L16648 — CSM-1B TTS §135 FIXED
- L16692 — Dia 1.6B TTS port
- L16868 — dots.tts #200 SHIPPED
- L17241 — higgs-audio-v3-stt SHIPPED
- L17372 — indextts long-ref crash FIXED
- L17611 — TADA TTS time embedding bugs
- L17659 — Four bugs that together caused empty/garbage transcripts; all patched in src/vibevoice.cpp +…
- L17884 — "Verified byte-identical at 225 s" verified a clip, not a length — and a decoder that drops spans needs a repair pass, not a better cap

## Cross-reference by model / family

A section is listed when its heading names the family, or its body mentions it
at least 3 times. Sections appear under several families; this is a
lookup, not a partition.

- **chatterbox** (26) — L2203, L2388, L5419, L5681, L5732, L5764, L5803, L5907, L5960, L6066, L6169, L6235, L6310, L6358, L8507, L9294, L12919, L13967, L14356, L14416, L14875, L15664, L16274, L16312, L16868, L17942
- **cosyvoice3** (9) — L587, L8901, L12919, L13967, L14416, L15293, L15526, L16485, L16513
- **vibevoice** (14) — L512, L1663, L2570, L5138, L5907, L8418, L12770, L12805, L12848, L13524, L14980, L15000, L15231, L17659
- **tada** (9) — L993, L1366, L1710, L1760, L1774, L12919, L13182, L15908, L17611
- **whisper** (22) — L187, L1879, L2857, L3657, L3841, L3914, L4050, L4557, L5498, L7469, L7837, L12681, L14539, L15045, L15088, L15154, L15596, L16144, L16485, L17241, L17301, L17417
- **parakeet / TDT** (17) — L720, L1879, L1912, L2981, L3914, L4260, L6358, L8352, L10670, L10755, L11795, L13353, L14496, L14539, L15596, L17417, L17884
- **moss** (13) — L447, L643, L1135, L1337, L1512, L1518, L1809, L1834, L1859, L2981, L14159, L14914, L14932
- **firered** (9) — L1304, L4466, L4557, L5138, L5305, L6169, L13300, L15045, L15088
- **funasr / sensevoice / SANM** (2) — L8901, L11165
- **kyutai / moshi / mimi** (7) — L1524, L1588, L4423, L11335, L12109, L12265, L16648
- **dia** (5) — L1266, L11294, L11369, L11589, L16692
- **parler** (1) — L11663
- **speecht5** (2) — L11294, L11589
- **bark** (1) — L11897
- **pocket-tts** (3) — L12109, L12265, L17486
- **melotts / VITS** (5) — L1430, L12358, L12483, L12898, L15966
- **fastpitch** (1) — L11423
- **indextts** (1) — L7837
- **orpheus / SNAC** (1) — L2760
- **voxcpm2** (4) — L8507, L8711, L8834, L17683
- **openvoice2** (1) — L12444
- **glm-asr** (1) — L13706
- **qwen3** (17) — L1337, L3211, L3914, L4557, L7236, L7837, L8507, L8901, L10883, L13665, L13763, L13856, L14539, L14875, L15293, L17301, L17804
- **madlad / T5** (1) — L7131
- **mini-omni2** (2) — L2857, L2872
- **titanet / speaker-id** (1) — L8304
- **audioseal** (1) — L12504
- **cohere** (7) — L267, L3314, L10819, L12475, L14539, L14819, L16367
- **voxtral** (11) — L3211, L3314, L3362, L3522, L3914, L10755, L10819, L10846, L10981, L15664, L16868
- **granite** (3) — L1954, L3211, L14539
- **ark-asr** (2) — L16144, L16868
- **higgs-stt** (2) — L643, L17241
- **dots-tts** (2) — L1518, L16868
- **irodori** (2) — L1395, L14656
- **f5-tts** (1) — L13998
- **kokoro** (4) — L360, L7236, L15293, L15966
- **zonos** (1) — L11663
- **omniasr / omnivoice** (6) — L187, L1090, L4557, L13795, L13823, L14209
- **glm / mimo / mega** (3) — L11011, L13706, L13763
