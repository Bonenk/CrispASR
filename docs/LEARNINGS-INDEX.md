# LEARNINGS.md — index

GENERATED by `tools/gen-learnings-index.py`. Do not hand-edit; re-run it.

`LEARNINGS.md` is ~800 KB and nobody reads it whole. Scan this index, then jump:

```bash
grep -n "<heading text>" LEARNINGS.md   # then Read with that offset
```

Every heading states its lesson in full, so **the index is the summary** — if a
line does not sound relevant, neither is the section under it. `L<n>` is the line
in LEARNINGS.md at generation time; re-run the tool if the numbers look off.

Roughly half the file is transferable method (the topic groups) and half is
per-backend porting detail (Per-model port notes + the family cross-reference).
Reach for the topic groups when you are stuck on a *technique*, and the
cross-reference when you already know which model you are touching.

## Index by topic (280 lessons)

**Security & untrusted input** (2)

- L1506 — Two more untrusted-input lessons: an HTTP server reads the body before your auth check, and a parser that abort()s on malformed input is a DoS a fuzzer finds in seconds
- L1533 — Every hand-rolled file parser sizes a buffer from an untrusted length field — that is the #1 memory-safety bug class, and a multi-agent audit finds them fast

**Compliance, provenance & marking (EU AI Act)** (7)

- L249 — A watermarked TTS output cannot be A/B'd with `cmp` — and whisper LID is not an accent metric
- L809 — Moving a release tag is CLEAN when the fix touches only release TOOLING — the test is provenance, not "never move a tag"
- L10881 — Always rebuild the box-under-test before benchmarking
- L15970 — EU AI Act audit — provenance not suffix
- L16028 — EU AI Act — a PRESET voice can be a real person
- L16478 — consent record — BIND to the audio, don't chain the log
- L17773 — watermark detector — swap the STATISTIC, not the threshold

**Build, CI, release & packaging** (21)

- L153 — A packaging step that destroys the state a later step reads — and scripts that only run in a release job
- L457 — A guard job that runs ONE compiler family guards one compiler family
- L745 — A green release job is not a shipped artifact — verify what you DELIVER, not what the workflow says
- L874 — "Linked in CMake" is NOT evidence the code SHIPS — the linker drops an object nothing references
- L1094 — Path-filtered CI workflows ROT — the first re-triggering change inherits ALL the debt accumulated since the last run
- L1114 — Scoped `ctest -R` on a PARTIALLY BUILT tree has two name-collision traps: configure-time add_test and catch_discover NOT_BUILT placeholders
- L1399 — Porting a libllama-hosted reference onto our in-house Qwen3 was clean on first contact; and Kaggle `kernels_logs()` is the escape hatch when the output is page-capped
- L1457 — ggml disables CUDA graphs below Ampere (cc < 800) — an Ampere-only re-warm bug can't be reproduced on Kaggle's P100/T4
- L1756 — Two concurrent HuggingFace uploads from one machine cause spurious mid-batch failures — serialize them (#192 aligner re-ship)
- L2225 — Never benchmark on a near-full disk — SIGBUS + nondeterminism masquerade as a code signal (§192)
- L2301 — Kaggle: datasets are per-account, and the ccache dataset has a required shape (§213)
- L2723 — Kaggle regression kernels: write output incrementally, crash-guard everything
- L2850 — Regenerate Go cgo LDFLAGS from a *linux-equivalent* config, never macOS
- L2865 — cmake-js defaults its build dir to `build/` — point it elsewhere (§166)
- L3019 — HF Space is a separate, FLAT repo that silently drifts from `hf-space/`
- L4322 — Windows / MSVC portability
- L5560 — GitHub Actions workflow triggers — `master` → `main` rename gotcha
- L5706 — libcrispasr.a + libcommon.a both define stb_vorbis / miniaudio impl (Linux ld dies)
- L10945 — Kaggle as a batch-rebake target: seven fragilities the script has to work around
- L14389 — CI has no NVIDIA GPU — a CUDA-only graph path (mimo RVQ) is guarded by an on-Kaggle exact-parity smoke, not CI (#309)
- L15216 — #313 Rust crates on crates.io via git dep

**Multi-surface wiring — CLI / C ABI / bindings / server** (25)

- L20 — Splitting a path in half drops its prologue and its epilogue, and "disjoint state" has to include what the CALLER does between the halves
- L378 — A gate that ANDs "both sides known" is only as alive as its weakest detector
- L617 — A new REQUIRED request field is a breaking API change on a schedule you don't control — deny the sub-feature, don't refuse the request
- L705 — A hardcoded decode cap silently ignores --max-new-tokens — and forwarding it naively SHRINKS a backend
- L782 — A capability flag is a PROMISE the backend must keep — an unimplemented cap disables the safety nets keyed off it
- L1076 — Two writers on one output field at different pipeline stages = an ordering bug — fix the ORDER, don't bolt on provenance
- L1135 — A binding wrapper that has never been EXECUTED is untested code — and parallel agents produce cross-cutting conflicts only the verifier can see
- L1725 — A persistent KV cache reused from a larger allocation makes local `max_ctx` the wrong stride (#171 VibeVoice server leak)
- L2632 — Backend-name guards must match the *registered* name, by prefix when aliased (#171, #174)
- L2766 — A feature has ~8 front-ends — wiring it into one isn't "done" (§166)
- L2792 — A dry-run "preview" must mirror the real resolver, or it lies (§166)
- L2877 — A WebSocket server that "works in the browser" can still be RFC-broken (§166)
- L2891 — Server warmup is the launch-time differentiator vs the CLI (#165)
- L2904 — Don't throw on the per-request server path; surface the real error (#165)
- L6231 — Chatterbox CAMPPlus phase 1 — Kaldi fbank front-end
- L8773 — voxcpm2 VAE — Python wrapper that captures kwargs without forwarding
- L13309 — 2026-07-02 — `--gpu-backend` was silently ignored (#214)
- L13997 — Before starting a "top-priority, profiled" perf target: check whether it's already been tried-and-rejected, and whether a parallel session owns it
- L14478 — A public API with no caller is a harness-blind zone announcing itself — and an ONNX export may FOLD a BatchNorm your C++ still expects as tensors
- L15150 — #311 --strict-pipeline SHIPPED
- L15293 — #315 vibevoice long-form ASR budget: PR fixed only session; CLI+server needed the adapter…
- L15329 — #324 server diarize DROPPED 59% of the transcript
- L17479 — Parakeet #208 long-audio + enc-cache collapse
- L17586 — session-state fixes + moonshine CPU-only SOLVED
- L17810 — Wyoming TTS marked nothing — FIXED 8f4197ec

**Parity, the diff harness & ground truth** (38)

- L82 — A guard has to assert the property the CONSUMER reads — segment spans are not cue times
- L422 — The diff harness starts where its INPUT starts — check what it is fed before trusting parity
- L649 — When a clone/zero-shot path misbehaves but the BAKED/preset path is clean, diff the two paths' DATA byte-for-byte — the difference IS the bug
- L912 — Cosine, correlation and peak-match are ALL scale-invariant — a uniform gain error passes every one of them
- L980 — A logits-level diff harness at cos 1.000000 says nothing about the TABLE that turns logits into labels
- L1055 — When you can't run the acceptance roundtrip, ship the path gated default-OFF + a provable-equivalence argument — don't block on the model
- L1152 — Deterministic DROPOUT of whole words/tokens (not garble) = a zeroed weight BLOCK in the shipped GGUF — scan converted tensors for zero-norm rows BEFORE debugging the port
- L1197 — A TTS AR model that "never stops" is often a WRONG-SAMPLING-PARAMS bug, not a stop-head/port bug — read the model card's generation params
- L1492 — Validating a TTS perf change by WAV correlation is invalid — melotts `--seed` isn't deterministic; use ASR round-trip or a deterministic-ASR bit-check
- L1574 — MelsTime mel layout IS ggml Conv1d input layout — transposing corrupts with cos ~0.3, not a gradual drift
- L1619 — Baked mel filterbank layout MUST match core_mel's `fb_layout` — a transpose produces cos_min ≈ −0.15, not a gradual drift
- L1822 — A production default that diverges from the upstream reference is a whole class of bug the stage-cosine diff can't see (#192 TADA)
- L1862 — A forced aligner is not an ASR model — its free CTC argmax is all-blank (#192)
- L1871 — When the engine and the PyTorch reference produce the SAME wrong output, the engine is right — the harness input (usually the prompt) is the bug (moss-transcribe)
- L1896 — An input feature the checkpoint wasn't trained with is worse than nothing — the moss-transcribe time-marker experiment (#218)
- L1921 — Stage the PyTorch reference load — encoder and LM as separate standalone modules, never the monolithic `*ForCausalLM` (moss-transcribe)
- L1941 — Parakeet single-pass DROPS whole sections of long audio — chunk-and-merge, and pick the reference clip carefully (#208b)
- L2237 — Always A/B against the *upstream reference*, not just your own option matrix (§216)
- L2288 — TTS WAV md5 is only a valid parity gate with a pinned seed
- L2709 — Regression transcripts need WER tolerance, not byte-exact match (#92)
- L3719 — Methodical debugging of ported models against ground truth
- L5826 — Chatterbox base T3 sampler parity — Gumbel-max → torch.multinomial
- L5865 — Chatterbox HiFT vocoder parity nits
- L8896 — Diff-harness "drift" is mostly the GGUF quant, not a code bug
- L12171 — Pocket TTS — 12 bugs from stub to cos=0.999997
- L13586 — ASR roundtrip does not validate audio ONSETS; reproduce with the original pipeline before debugging your runtime
- L13630 — Porting the missing sub-model is necessary but not sufficient — hunt the hardcoded "unconditional" defaults that ignore it
- L13703 — Python reference dumpers must apply identical audio conditioning as the C++ runtime
- L13720 — A `frames/token` (or chars/sec) length heuristic silently truncates diffusion TTS — run the duration predictor the model already ships
- L13825 — A faithful port can still loop: get the bf16 blueprint's behaviour on the SAME audio before hunting a runtime bug
- L13885 — Generation-config defaults are part of the blueprint contract — a masked-iterative TTS with the wrong sampler knobs degenerates to SILENCE, not to bad audio
- L14029 — CosyVoice3 CFM Euler steps: 6 is the sweet spot, and validate it with log-mel corr vs full-steps — NOT ASR roundtrip
- L14221 — Exact greedy code-parity is unachievable for a QUANTIZED AR audio LM — confirm the port with a step-0 logit-rank probe, not byte-exact codes
- L14271 — A GPU-vs-reference RTF gap that survives kernel parity is per-step HOST detours — fuse them into the step graph; unified-memory Metal hides exactly this class of cost
- L14364 — A normalization stage amplifies upstream float error — input-align it before trusting its cos (§248 MBR)
- L15502 — #333 madlad400 quants + T5 parity
- L16374 — chatterbox hift_pcm(ref_mel) cos≈0.879 was a diff-harness layout bug (source_stft fed transposed), not a…
- L18004 — Chatterbox Multilingual V3: checkpoint names, quant hashes, and a non-silent clone are not parity

**ggml graphs, allocation & caching** (43)

- L688 — Reusing a cached scheduler graph across `sched_reset`/`alloc` cycles is CPU-safe but SIGSEGVs on GPU — the reused input tensor is bound to the prior cycle's freed buffer
- L1258 — Metal-validated GPU TTS graph paths DID transfer cleanly to CUDA — the stricter-CUDA risk is real but not universal
- L1294 — BatchNorm-fold must respect the conv tensor's dtype — and a per-backend fix usually has siblings
- L1328 — "Migrate host KV to device-resident" is a ~1% win on a compute-bound transformer decoder — measure the fraction before the rewrite
- L1628 — Never cache a ggml compute graph across calls that share a scheduler with a larger graph — the allocator regrows and frees the cached graph's GPU buffers
- L1639 — Flow-matching DiT with JointAttention REQUIRES attention masking even for unconditional generation — skipping masked KV tokens silently corrupts output (2026-07)
- L1974 — A cached cgraph is NOT re-entrant with ggml_backend_sched — the 2nd reuse silently corrupts (#208)
- L2016 — On Metal a cached decode graph is dispatch-bound, not alloc-bound — measure the step's parts, don't trust wall time (§210, PR #207)
- L2173 — Precomputed attention masks in a GGUF are dead weight if the runtime rebuilds them (§192)
- L2341 — Cross-attention KV is a free F16 win — encoder-decoder backends (§176i)
- L2369 — Localizing a GPU miscompute with CPU-vs-Metal diffing — the ggml_backend_sched weight-less-first-op trap (§206)
- L2523 — A byte-identical standalone reproducer is the only way to (dis)prove a "ggml bug" (§203)
- L2553 — In-place device-KV decode graphs on Metal: three hazards (§176b+c, §203)
- L2661 — Never pass `ggml_graph_get_tensor` directly to `ggml_backend_tensor_set` (#164)
- L2681 — `rope_theta=0` means "no positional encoding" — skip RoPE entirely (#164)
- L2690 — `ggml_siglu` ≠ PyTorch `glu` — the gate/value halves are SWAPPED (#81)
- L2759 — Asymmetric rel-pos shift uses the same formula as symmetric (#81)
- L2804 — Graph input tensors that nothing consumes are invisible to `ggml_graph_get_tensor` (#164)
- L2837 — `ggml_backend_cpu_set_n_threads` asserts CPU — guard it after `init_best()`
- L2950 — Decompose ConvTranspose1d → mul_mat + col2im_1d; port col2im per-backend (#155)
- L2990 — Beam-search KV snapshots must stay on-device (#161)
- L3273 — ggml / inference engine
- L3845 — ggml graph allocation: gallocr vs compute_with_ctx
- L5193 — 2026-04-22 - No-gpu mode must gate `ggml_backend_load_all()`
- L7298 — 2026-05-05 — ggml fork patches we carry (must re-apply on every ggml bump)
- L8569 — voxcpm2 perf — per-step ggml graphs, Metal, SIMD layouts
- L8853 — ggml broadcast hides size-mismatch bugs
- L11073 — ggml scheduler tightened cross-backend tensor resolution between §56 and 2026-05-26 (PLAN #115)
- L12327 — Pocket TTS — manual CPU → ggml compute graph rewrite
- L12566 — AudioSeal ggml port — ggml_pad_ext convention trap
- L12643 — GPU weight mirrors for mixed legacy / graph codebases
- L12767 — ggml_graph_get_tensor hash invalidated by ggml_gallocr
- L12960 — read_tensor_f32 weight pre-cache for VITS-family TTS
- L12973 — Single-token embed graph elimination for LLM-ASR
- L13487 — A once-allocated-then-reused bucket step graph is a CUDA-graph-capture hazard: reset+alloc the sched EVERY step
- L13546 — A cached cgraph is invalidated by ANY other graph on its scheduler — reset+alloc per step is NOT enough
- L13646 — TTS code predictor: the dispatch overhead is real, but "fuse into one graph" and "skip the reset" are BOTH wrong — the fix is a sched-free persistent graph
- L13715 — Any T-sized `ggml_view` into a fixed-length weight table must be bounded by the tensor's real length — long inputs overflow it and abort
- L13857 — gallocr does NOT protect input-flagged tensors across computes — re-set EVERY input before EVERY compute of a persistent graph
- L13918 — Vocoder/codec conv graphs hide three graph-construction wastes that dwarf the actual conv FLOPs — profile per-node before assuming it's inherent
- L13965 — Before optimizing a graph's *dispatch*, split host-encode vs GPU-execute — and never trust a GPU benchmark taken under load
- L14060 — A "GPU" model can be silently running on CPU — audit for hot graphs pinned to backend_cpu and CLI adapters that never forward use_gpu
- L17548 — PR #244 dequant-cache fix MERGED

**GPU portability — Metal / CUDA / Vulkan** (24)

- L1233 — When the full system needs an unavailable resource (model / GPU), factor the risky logic into a pure helper and prove IT on synthetic data
- L1348 — Verify a roadmap "broken/OPEN" claim empirically before implementing it — the codebase may have outgrown the note
- L1428 — A Metal masked-attention-padding penalty does NOT transfer to CUDA, and a bucket-width change is byte-identical on Metal/CPU but not on CUDA
- L1475 — ggml-metal im2col starves batch-1 convs — 3–11 threads/threadgroup at N=1, ~40× below bandwidth
- L2137 — A "garbled GPU output" bug can live entirely downstream — A/B the GPUs before localizing (§192)
- L2191 — MoltenVK `mul_mm`/`mul_mat_vec` downconvert src0 to f16 regardless of stored dtype (§192)
- L2202 — Vulkan has no `REPEAT f16→f16` — cast K/V to F32 *before* the GQA repeat (§192)
- L2215 — On macOS, GGML_METAL is default-ON and silently wins over Vulkan (§192)
- L2265 — §214 Metal batched (B=2) quantized mat-vec ≠ the single-token PREC_F32 path
- L2476 — Metal's q8_0 mat-vec kernel requantizes activations to q8 and ignores the F32 prec hint (§205)
- L2974 — Measuring GPU perf on a thermally-throttling laptop GPU
- L9219 — WDDM idle-clock-state hysteresis on consumer/laptop NVIDIA SKUs
- L9356 — Chatterbox #83 Round 9 — S3Gen UNet GPU drift on Metal
- L10609 — FA per-head additive mask CUDA kernel — what the upstream signature already gave us
- L11227 — funasr CUDA !-loop — all-NaN prefill logits (issue #125, §136)
- L12537 — Cohere flash-attn crossover
- L12597 — conv_transpose_1d GPU TDR — naive loop is O(IL), not O(K/s0)
- L12799 — flash_attn_ext vs scalar attention — numerical divergence
- L12910 — VibeVoice TTS garbles only on AMD RDNA4: coopmat2 flash-attention
- L14095 — Micro-optimizing an already-BLAS'd path with a GPU-ggml port is usually a dud — check for existing Accelerate/cblas and measure encode-vs-execute BEFORE porting
- L14418 — "Backend miscomputes my pipeline" ≠ "op X is broken": arbitrate with test-backend-ops, and beware aggregate precision (#304 native-Vulkan post-mortem)
- L16336 — Chatterbox GPU UNet performance fix
- L16575 — cosyvoice3 \"CUDA test FAIL\" was a registry-alias + flow-quant discovery bug, NOT a CUDA bug
- L17866 — "GPU picks a different token than CPU" is usually NOT a miscompute in an AR audio model — dump the LOGITS, and don't reach for the repetition detector

**Quantization** (10)

- L1692 — Auditing CrispASR against CrispEmbed's bug classes: a weight reader with no quantized branch fails silently (2026-07)
- L1772 — Quantizing a forced-aligner: q8 everywhere (incl. lm_head) is bit-identical; q4 on the encoder is not (#192 TADA aligner)
- L2822 — Orpheus/SNAC TTS: `token_embd` must stay F16 for sub-Q8 quants
- L3424 — Quantisation and memory
- L3699 — Quantization
- L12528 — F16 precision loss in deep WaveNet stacks
- L12545 — MeloTTS + BERT quantization — what works and what doesn't
- L13639 — Re-quantized GGUFs on HF must be re-baked when the quantizer adds carve-outs
- L13727 — Sub-8-bit quantization of an audio tower fails as BEHAVIORAL collapse (loops, empty output) — and there is no per-block cliff to bisect
- L17363 — imatrix quant SHIPPED

**Perf measurement & A/B discipline** (8)

- L1366 — Per-step matvec dispatch overhead is LOAD-DEPENDENT — a "win" measured on a busy box is mostly a contention artifact
- L1836 — Best-of-N only helps if the scorer measures what you care about — TADA's reconstruction scorer is blind to duration outliers (#192)
- L3460 — CPU vs ONNX vs PyTorch baselines
- L3903 — Performance: what faster-whisper / insanely-fast-whisper do
- L5200 — 2026-04-23 - FireRed decoder optimization triage
- L10908 — Distinguishing "slow run" from "hung run" — CPU time ≪ wall time is the signal
- L11043 — Cross-backend bug-sweep methodology — pair the cap survey with an empirical A/B
- L12981 — §176 runtime optimization audit methodology

**Long-form audio, VAD & chunking** (8)

- L1597 — LLM-ASR (SALM) instruction-echo on short windows is genuine model behaviour, not a port bug — gate the input, don't chase the prompt
- L1650 — The Mimi codec transformer must be causal — non-causal silently truncates long audio; the >250-frame WER A/B settled it (2026-07)
- L3043 — VAD + chunking
- L3976 — VAD integration and long audio
- L10732 — TDT single-pass over a full long utterance is numerically fragile to codec-level audio noise
- L14840 — #227 "--vad-import wants ggml-tiny" = LID not VAD
- L15062 — #300 streaming diarization SHIPPED
- L15658 — #89 parakeet-ja long-form FIXED

**Tokenizers, prompts, language & text** (17)

- L509 — MOSS-TTS-Local 4B stop runaway was a PROMPT-TOKENIZATION bug — non-compositional BPE, not the forward (#249, 2026-07)
- L559 — Cross-lingual TTS needs the target language plumbed through /v1/audio/speech (#249/#304, 2026-07)
- L1848 — A converter that embeds a tokenizer MUST embed the merges — a silent `try/except: pass` byte-fallback breaks everything downstream (#192)
- L3546 — Language handling
- L5367 — FireRedPunc / fullstop-punc — BERT punctuation restoration
- L6372 — Chatterbox 24 kHz prompt mel — module 4 phase 3
- L7531 — Text LID via fastText — GlotLID-V3 + LID-176
- L7742 — Text LID via CLD3 — Google compact language detector
- L12832 — VibeVoice TTS: missing BPE merges still need BPE behavior
- L13362 — 2026-07 — SentencePiece tokenizer taxonomy: greedy longest-match is wrong for BOTH Unigram and BPE
- L13619 — SentencePiece `byte_fallback` is not optional decoration — OOV emoji/symbols must become `<0xHH>` byte tokens, and a `utf8_aligned` Viterbi dead-ends at a multi-byte lead without it
- L13768 — The prompt contract is part of the port: a specials-only tokenizer silently sent EVERY glm-asr prompt instruction-less
- L13803 — Unigram run-length loop metrics pass 2-gram cycles — and a "raw" baseline is only raw if the disable gate actually exists
- L14976 — #249 MOSS-Local 4B stop-runaway = PROMPT tokenization bug
- L15355 — #329 TTS target language
- L16429 — cohere language whitelist + probe LID
- L16547 — CV3 phase 6 speech_tokenizer_v3 ggml port

**Mel, codecs & the audio front-end** (15)

- L1586 — A one-frame error in a codec ENCODER shifts every RoPE position and shows up as an onset artifact, not a gradual drift — and a padding scheme faithful to one codec can be wrong for another
- L2450 — A glibc-only crash reproduces on macOS under AddressSanitizer — and the bug is often a non-power-of-two FFT (§205)
- L2741 — Streaming conv modules need cached left context, not zero-padding (#81)
- L2919 — FFT size must match upstream exactly (Mini-Omni2 / Whisper mel)
- L3228 — mel / preprocessor
- L3376 — Mel spectrograms
- L3514 — Audio format lessons
- L4485 — Kyutai STT: causal padding, interleaved RoPE, and codec-based ASR
- L4619 — FireRedVAD: FSMN Conv1d replication
- L6420 — Chatterbox atomic native voice clone — the resampler + 5-cond install
- L11397 — moshi / Mimi RVQ codebooks: decode uses embed_sum / cluster_usage
- L12420 — MeloTTS (VITS2) — from zero to BERT conditioning
- L14937 — #245/§232 qwen3-tts CP_DIRECT + codec FASTCONV DONE
- L17262 — glint MP3+AAC TTS output §225 SHIPPED
- L17745 — voxcpm2 CausalTransposeConv1d kwargs gotcha

**Process, triage & documentation discipline** (18)

- L481 — When instrumenting the suspect file produces NO output, you are editing the wrong file — look for a second copy
- L574 — "The model emits it inline as text" is a claim to VERIFY, not to document — structured data you never parsed looks identical to a model limitation
- L831 — A positional arg landing on the WRONG parameter is invisible in review and baked into the weights
- L1036 — PLAN "OPEN" items are frequently already shipped — audit against the CODE, never the prose
- L1274 — Audit a whole roadmap CLUSTER in one measure-first pass before implementing any of it
- L1580 — Handover docs can be dangerously stale — read the actual Python source, not the handover
- L2329 — DRY shared headers can exist for months before callers migrate (§175)
- L3622 — Regression testing discipline
- L3649 — Specific bugs that cost us a day each
- L4112 — CLI ↔ library DRY refactor
- L5676 — Audit script ≠ behavior test
- L10817 — Long-form ASR has three distinct failure classes, not one (2026-05-25, generalising issue #89)
- L12743 — beam_size default — greedy vs beam-5
- L12867 — VibeVoice TTS start clicks: distinguish decoder PCM from CLI post-processing
- L13415 — Issue #89 close-out: four transferable lessons
- L13710 — Issue triage discipline: check the codebase before leaving issues open
- L14337 — `git apply --3way` STAGES its result — a later `git add X && git commit` sweeps it up
- L16615 — CrispEmbed #31 WASM OCR e2e

**Per-model port notes** (44)

- L329 — A model's capability list cannot be inferred from its vocabulary — and forcing a fake list does not simulate having the capability
- L2934 — Multi-stream token architecture (Mini-Omni2)
- L3584 — Model architecture comparisons
- L4528 — FireRedASR: Conformer encoder debugging
- L5481 — TTS / Vocoder (Chatterbox HiFTGenerator)
- L5743 — Chatterbox-Turbo conformer encoder — ggml layout traps
- L5794 — Chatterbox repaired GGUF split — stage-specific regen
- L5969 — Chatterbox voice cloning — bake to GGUF, load via `--voice`
- L6022 — Chatterbox VoiceEncoder native port — module 2 of voice cloning
- L6128 — Chatterbox S3Tokenizer V2 native port — module 3 of voice cloning
- L6297 — Chatterbox CAMPPlus phase 2 — TDNN forward
- L7193 — T5-family translation runtime traps (May 2026, MADLAD-400 debugging)
- L7899 — IndexTTS-1.5 TTS backend
- L8366 — Speaker verification — TitaNet
- L8414 — Parakeet-TDT greedy decode — blank + duration=0
- L8480 — VibeVoice 1.5B TTS voice cloning: acoustic + semantic dual encoder
- L8963 — SANM-encoder family (FunASR / SenseVoice / CosyVoice)
- L11356 — Round 10 — SpeechT5 + Dia TTS backend ports (2026-05-31/06-01)
- L11431 — Dia 1.6B TTS — what ACTUALLY fixed it
- L11485 — FastPitch TTS — non-autoregressive parallel TTS port (§133, 2026-06-02/03)
- L11651 — SpeechT5 TTS decoder — what ACTUALLY fixed it
- L11725 — Parler TTS — T5 + MusicGen decoder + DAC 44 kHz
- L11857 — MAES beam search for TDT transducers
- L11959 — Bark TTS — what ACTUALLY fixed it
- L12506 — OpenVoice2 voice cloning — ggml data layout
- L13244 — TADA encoder port: staged model loading on constrained RAM (§221)
- L14558 — #195 parakeet-ctc-1.1b-ja CTC routing
- L14601 — #205 --max-len text-split + granite-plus
- L14718 — #221 irodori voice cloning SHIPPED
- L14881 — #231 cohere-arabic loop = corrupt GGUF
- L14994 — #249 MOSS voice cloning SHIPPED
- L15042 — vibevoice #299 runtime-reproduced
- L15107 — #308 whisper subtitles double-caps
- L15588 — #334 cosyvoice3 clone bugs
- L15726 — #93 voxtral-tts SHIPPED
- L16206 — ARK-ASR-3B port SHIPPED
- L16710 — CSM-1B TTS §135 FIXED
- L16754 — Dia 1.6B TTS port
- L16930 — dots.tts #200 SHIPPED
- L17303 — higgs-audio-v3-stt SHIPPED
- L17434 — indextts long-ref crash FIXED
- L17673 — TADA TTS time embedding bugs
- L17721 — Four bugs that together caused empty/garbage transcripts; all patched in src/vibevoice.cpp +…
- L17946 — "Verified byte-identical at 225 s" verified a clip, not a length — and a decoder that drops spans needs a repair pass, not a better cap

## Cross-reference by model / family

A section is listed when its heading names the family, or its body mentions it
at least 3 times. Sections appear under several families; this is a
lookup, not a partition.

- **chatterbox** (26) — L2265, L2450, L5481, L5743, L5794, L5826, L5865, L5969, L6022, L6128, L6231, L6297, L6372, L6420, L8569, L9356, L12981, L14029, L14418, L14478, L14937, L15726, L16336, L16374, L16930, L18004
- **cosyvoice3** (9) — L649, L8963, L12981, L14029, L14478, L15355, L15588, L16547, L16575
- **vibevoice** (14) — L574, L1725, L2632, L5200, L5969, L8480, L12832, L12867, L12910, L13586, L15042, L15062, L15293, L17721
- **tada** (9) — L1055, L1428, L1772, L1822, L1836, L12981, L13244, L15970, L17673
- **whisper** (22) — L249, L1941, L2919, L3719, L3903, L3976, L4112, L4619, L5560, L7531, L7899, L12743, L14601, L15107, L15150, L15216, L15658, L16206, L16547, L17303, L17363, L17479
- **parakeet / TDT** (18) — L20, L782, L1941, L1974, L3043, L3976, L4322, L6420, L8414, L10732, L10817, L11857, L13415, L14558, L14601, L15658, L17479, L17946
- **moss** (13) — L509, L705, L1197, L1399, L1574, L1580, L1871, L1896, L1921, L3043, L14221, L14976, L14994
- **firered** (9) — L1366, L4528, L4619, L5200, L5367, L6231, L13362, L15107, L15150
- **funasr / sensevoice / SANM** (2) — L8963, L11227
- **kyutai / moshi / mimi** (7) — L1586, L1650, L4485, L11397, L12171, L12327, L16710
- **dia** (5) — L1328, L11356, L11431, L11651, L16754
- **parler** (1) — L11725
- **speecht5** (2) — L11356, L11651
- **bark** (1) — L11959
- **pocket-tts** (3) — L12171, L12327, L17548
- **melotts / VITS** (5) — L1492, L12420, L12545, L12960, L16028
- **fastpitch** (1) — L11485
- **indextts** (1) — L7899
- **orpheus / SNAC** (1) — L2822
- **voxcpm2** (4) — L8569, L8773, L8896, L17745
- **openvoice2** (1) — L12506
- **glm-asr** (1) — L13768
- **qwen3** (17) — L1399, L3273, L3976, L4619, L7298, L7899, L8569, L8963, L10945, L13727, L13825, L13918, L14601, L14937, L15355, L17363, L17866
- **madlad / T5** (1) — L7193
- **mini-omni2** (2) — L2919, L2934
- **titanet / speaker-id** (1) — L8366
- **audioseal** (1) — L12566
- **cohere** (7) — L329, L3376, L10881, L12537, L14601, L14881, L16429
- **voxtral** (11) — L3273, L3376, L3424, L3584, L3976, L10817, L10881, L10908, L11043, L15726, L16930
- **granite** (3) — L2016, L3273, L14601
- **ark-asr** (2) — L16206, L16930
- **higgs-stt** (2) — L705, L17303
- **dots-tts** (2) — L1580, L16930
- **irodori** (2) — L1457, L14718
- **f5-tts** (1) — L14060
- **kokoro** (4) — L422, L7298, L15355, L16028
- **zonos** (1) — L11725
- **omniasr / omnivoice** (6) — L249, L1152, L4619, L13857, L13885, L14271
- **glm / mimo / mega** (3) — L11073, L13768, L13825
