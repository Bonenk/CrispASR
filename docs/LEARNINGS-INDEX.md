# LEARNINGS.md — index

GENERATED by `tools/gen-learnings-index.py`. Do not hand-edit; re-run it.

`LEARNINGS.md` is ~800 KB and nobody reads it whole. Scan this index, then jump:

```bash
grep -n "<heading text>" LEARNINGS.md   # then Read with that offset
```

Every heading states its lesson in full, so **the index is the summary** — if a
line does not sound relevant, neither is the section under it. `L<n>` is the line
in LEARNINGS.md at generation time; re-run the tool if the numbers look off.

Roughly half the file is transferable method (the topic groups) and half is
per-backend porting detail (Per-model port notes + the family cross-reference).
Reach for the topic groups when you are stuck on a *technique*, and the
cross-reference when you already know which model you are touching.

## Index by topic (279 lessons)

**Security & untrusted input** (2)

- L1427 — Two more untrusted-input lessons: an HTTP server reads the body before your auth check, and a parser that abort()s on malformed input is a DoS a fuzzer finds in seconds
- L1454 — Every hand-rolled file parser sizes a buffer from an untrusted length field — that is the #1 memory-safety bug class, and a multi-agent audit finds them fast

**Compliance, provenance & marking (EU AI Act)** (7)

- L170 — A watermarked TTS output cannot be A/B'd with `cmp` — and whisper LID is not an accent metric
- L730 — Moving a release tag is CLEAN when the fix touches only release TOOLING — the test is provenance, not "never move a tag"
- L10802 — Always rebuild the box-under-test before benchmarking
- L15891 — EU AI Act audit — provenance not suffix
- L15949 — EU AI Act — a PRESET voice can be a real person
- L16399 — consent record — BIND to the audio, don't chain the log
- L17694 — watermark detector — swap the STATISTIC, not the threshold

**Build, CI, release & packaging** (21)

- L74 — A packaging step that destroys the state a later step reads — and scripts that only run in a release job
- L378 — A guard job that runs ONE compiler family guards one compiler family
- L666 — A green release job is not a shipped artifact — verify what you DELIVER, not what the workflow says
- L795 — "Linked in CMake" is NOT evidence the code SHIPS — the linker drops an object nothing references
- L1015 — Path-filtered CI workflows ROT — the first re-triggering change inherits ALL the debt accumulated since the last run
- L1035 — Scoped `ctest -R` on a PARTIALLY BUILT tree has two name-collision traps: configure-time add_test and catch_discover NOT_BUILT placeholders
- L1320 — Porting a libllama-hosted reference onto our in-house Qwen3 was clean on first contact; and Kaggle `kernels_logs()` is the escape hatch when the output is page-capped
- L1378 — ggml disables CUDA graphs below Ampere (cc < 800) — an Ampere-only re-warm bug can't be reproduced on Kaggle's P100/T4
- L1677 — Two concurrent HuggingFace uploads from one machine cause spurious mid-batch failures — serialize them (#192 aligner re-ship)
- L2146 — Never benchmark on a near-full disk — SIGBUS + nondeterminism masquerade as a code signal (§192)
- L2222 — Kaggle: datasets are per-account, and the ccache dataset has a required shape (§213)
- L2644 — Kaggle regression kernels: write output incrementally, crash-guard everything
- L2771 — Regenerate Go cgo LDFLAGS from a *linux-equivalent* config, never macOS
- L2786 — cmake-js defaults its build dir to `build/` — point it elsewhere (§166)
- L2940 — HF Space is a separate, FLAT repo that silently drifts from `hf-space/`
- L4243 — Windows / MSVC portability
- L5481 — GitHub Actions workflow triggers — `master` → `main` rename gotcha
- L5627 — libcrispasr.a + libcommon.a both define stb_vorbis / miniaudio impl (Linux ld dies)
- L10866 — Kaggle as a batch-rebake target: seven fragilities the script has to work around
- L14310 — CI has no NVIDIA GPU — a CUDA-only graph path (mimo RVQ) is guarded by an on-Kaggle exact-parity smoke, not CI (#309)
- L15137 — #313 Rust crates on crates.io via git dep

**Multi-surface wiring — CLI / C ABI / bindings / server** (24)

- L299 — A gate that ANDs "both sides known" is only as alive as its weakest detector
- L538 — A new REQUIRED request field is a breaking API change on a schedule you don't control — deny the sub-feature, don't refuse the request
- L626 — A hardcoded decode cap silently ignores --max-new-tokens — and forwarding it naively SHRINKS a backend
- L703 — A capability flag is a PROMISE the backend must keep — an unimplemented cap disables the safety nets keyed off it
- L997 — Two writers on one output field at different pipeline stages = an ordering bug — fix the ORDER, don't bolt on provenance
- L1056 — A binding wrapper that has never been EXECUTED is untested code — and parallel agents produce cross-cutting conflicts only the verifier can see
- L1646 — A persistent KV cache reused from a larger allocation makes local `max_ctx` the wrong stride (#171 VibeVoice server leak)
- L2553 — Backend-name guards must match the *registered* name, by prefix when aliased (#171, #174)
- L2687 — A feature has ~8 front-ends — wiring it into one isn't "done" (§166)
- L2713 — A dry-run "preview" must mirror the real resolver, or it lies (§166)
- L2798 — A WebSocket server that "works in the browser" can still be RFC-broken (§166)
- L2812 — Server warmup is the launch-time differentiator vs the CLI (#165)
- L2825 — Don't throw on the per-request server path; surface the real error (#165)
- L6152 — Chatterbox CAMPPlus phase 1 — Kaldi fbank front-end
- L8694 — voxcpm2 VAE — Python wrapper that captures kwargs without forwarding
- L13230 — 2026-07-02 — `--gpu-backend` was silently ignored (#214)
- L13918 — Before starting a "top-priority, profiled" perf target: check whether it's already been tried-and-rejected, and whether a parallel session owns it
- L14399 — A public API with no caller is a harness-blind zone announcing itself — and an ONNX export may FOLD a BatchNorm your C++ still expects as tensors
- L15071 — #311 --strict-pipeline SHIPPED
- L15214 — #315 vibevoice long-form ASR budget: PR fixed only session; CLI+server needed the adapter…
- L15250 — #324 server diarize DROPPED 59% of the transcript
- L17400 — Parakeet #208 long-audio + enc-cache collapse
- L17507 — session-state fixes + moonshine CPU-only SOLVED
- L17731 — Wyoming TTS marked nothing — FIXED 8f4197ec

**Parity, the diff harness & ground truth** (38)

- L20 — A guard has to assert the property the CONSUMER reads — segment spans are not cue times
- L343 — The diff harness starts where its INPUT starts — check what it is fed before trusting parity
- L570 — When a clone/zero-shot path misbehaves but the BAKED/preset path is clean, diff the two paths' DATA byte-for-byte — the difference IS the bug
- L833 — Cosine, correlation and peak-match are ALL scale-invariant — a uniform gain error passes every one of them
- L901 — A logits-level diff harness at cos 1.000000 says nothing about the TABLE that turns logits into labels
- L976 — When you can't run the acceptance roundtrip, ship the path gated default-OFF + a provable-equivalence argument — don't block on the model
- L1073 — Deterministic DROPOUT of whole words/tokens (not garble) = a zeroed weight BLOCK in the shipped GGUF — scan converted tensors for zero-norm rows BEFORE debugging the port
- L1118 — A TTS AR model that "never stops" is often a WRONG-SAMPLING-PARAMS bug, not a stop-head/port bug — read the model card's generation params
- L1413 — Validating a TTS perf change by WAV correlation is invalid — melotts `--seed` isn't deterministic; use ASR round-trip or a deterministic-ASR bit-check
- L1495 — MelsTime mel layout IS ggml Conv1d input layout — transposing corrupts with cos ~0.3, not a gradual drift
- L1540 — Baked mel filterbank layout MUST match core_mel's `fb_layout` — a transpose produces cos_min ≈ −0.15, not a gradual drift
- L1743 — A production default that diverges from the upstream reference is a whole class of bug the stage-cosine diff can't see (#192 TADA)
- L1783 — A forced aligner is not an ASR model — its free CTC argmax is all-blank (#192)
- L1792 — When the engine and the PyTorch reference produce the SAME wrong output, the engine is right — the harness input (usually the prompt) is the bug (moss-transcribe)
- L1817 — An input feature the checkpoint wasn't trained with is worse than nothing — the moss-transcribe time-marker experiment (#218)
- L1842 — Stage the PyTorch reference load — encoder and LM as separate standalone modules, never the monolithic `*ForCausalLM` (moss-transcribe)
- L1862 — Parakeet single-pass DROPS whole sections of long audio — chunk-and-merge, and pick the reference clip carefully (#208b)
- L2158 — Always A/B against the *upstream reference*, not just your own option matrix (§216)
- L2209 — TTS WAV md5 is only a valid parity gate with a pinned seed
- L2630 — Regression transcripts need WER tolerance, not byte-exact match (#92)
- L3640 — Methodical debugging of ported models against ground truth
- L5747 — Chatterbox base T3 sampler parity — Gumbel-max → torch.multinomial
- L5786 — Chatterbox HiFT vocoder parity nits
- L8817 — Diff-harness "drift" is mostly the GGUF quant, not a code bug
- L12092 — Pocket TTS — 12 bugs from stub to cos=0.999997
- L13507 — ASR roundtrip does not validate audio ONSETS; reproduce with the original pipeline before debugging your runtime
- L13551 — Porting the missing sub-model is necessary but not sufficient — hunt the hardcoded "unconditional" defaults that ignore it
- L13624 — Python reference dumpers must apply identical audio conditioning as the C++ runtime
- L13641 — A `frames/token` (or chars/sec) length heuristic silently truncates diffusion TTS — run the duration predictor the model already ships
- L13746 — A faithful port can still loop: get the bf16 blueprint's behaviour on the SAME audio before hunting a runtime bug
- L13806 — Generation-config defaults are part of the blueprint contract — a masked-iterative TTS with the wrong sampler knobs degenerates to SILENCE, not to bad audio
- L13950 — CosyVoice3 CFM Euler steps: 6 is the sweet spot, and validate it with log-mel corr vs full-steps — NOT ASR roundtrip
- L14142 — Exact greedy code-parity is unachievable for a QUANTIZED AR audio LM — confirm the port with a step-0 logit-rank probe, not byte-exact codes
- L14192 — A GPU-vs-reference RTF gap that survives kernel parity is per-step HOST detours — fuse them into the step graph; unified-memory Metal hides exactly this class of cost
- L14285 — A normalization stage amplifies upstream float error — input-align it before trusting its cos (§248 MBR)
- L15423 — #333 madlad400 quants + T5 parity
- L16295 — chatterbox hift_pcm(ref_mel) cos≈0.879 was a diff-harness layout bug (source_stft fed transposed), not a…
- L17925 — Chatterbox Multilingual V3: checkpoint names, quant hashes, and a non-silent clone are not parity

**ggml graphs, allocation & caching** (43)

- L609 — Reusing a cached scheduler graph across `sched_reset`/`alloc` cycles is CPU-safe but SIGSEGVs on GPU — the reused input tensor is bound to the prior cycle's freed buffer
- L1179 — Metal-validated GPU TTS graph paths DID transfer cleanly to CUDA — the stricter-CUDA risk is real but not universal
- L1215 — BatchNorm-fold must respect the conv tensor's dtype — and a per-backend fix usually has siblings
- L1249 — "Migrate host KV to device-resident" is a ~1% win on a compute-bound transformer decoder — measure the fraction before the rewrite
- L1549 — Never cache a ggml compute graph across calls that share a scheduler with a larger graph — the allocator regrows and frees the cached graph's GPU buffers
- L1560 — Flow-matching DiT with JointAttention REQUIRES attention masking even for unconditional generation — skipping masked KV tokens silently corrupts output (2026-07)
- L1895 — A cached cgraph is NOT re-entrant with ggml_backend_sched — the 2nd reuse silently corrupts (#208)
- L1937 — On Metal a cached decode graph is dispatch-bound, not alloc-bound — measure the step's parts, don't trust wall time (§210, PR #207)
- L2094 — Precomputed attention masks in a GGUF are dead weight if the runtime rebuilds them (§192)
- L2262 — Cross-attention KV is a free F16 win — encoder-decoder backends (§176i)
- L2290 — Localizing a GPU miscompute with CPU-vs-Metal diffing — the ggml_backend_sched weight-less-first-op trap (§206)
- L2444 — A byte-identical standalone reproducer is the only way to (dis)prove a "ggml bug" (§203)
- L2474 — In-place device-KV decode graphs on Metal: three hazards (§176b+c, §203)
- L2582 — Never pass `ggml_graph_get_tensor` directly to `ggml_backend_tensor_set` (#164)
- L2602 — `rope_theta=0` means "no positional encoding" — skip RoPE entirely (#164)
- L2611 — `ggml_siglu` ≠ PyTorch `glu` — the gate/value halves are SWAPPED (#81)
- L2680 — Asymmetric rel-pos shift uses the same formula as symmetric (#81)
- L2725 — Graph input tensors that nothing consumes are invisible to `ggml_graph_get_tensor` (#164)
- L2758 — `ggml_backend_cpu_set_n_threads` asserts CPU — guard it after `init_best()`
- L2871 — Decompose ConvTranspose1d → mul_mat + col2im_1d; port col2im per-backend (#155)
- L2911 — Beam-search KV snapshots must stay on-device (#161)
- L3194 — ggml / inference engine
- L3766 — ggml graph allocation: gallocr vs compute_with_ctx
- L5114 — 2026-04-22 - No-gpu mode must gate `ggml_backend_load_all()`
- L7219 — 2026-05-05 — ggml fork patches we carry (must re-apply on every ggml bump)
- L8490 — voxcpm2 perf — per-step ggml graphs, Metal, SIMD layouts
- L8774 — ggml broadcast hides size-mismatch bugs
- L10994 — ggml scheduler tightened cross-backend tensor resolution between §56 and 2026-05-26 (PLAN #115)
- L12248 — Pocket TTS — manual CPU → ggml compute graph rewrite
- L12487 — AudioSeal ggml port — ggml_pad_ext convention trap
- L12564 — GPU weight mirrors for mixed legacy / graph codebases
- L12688 — ggml_graph_get_tensor hash invalidated by ggml_gallocr
- L12881 — read_tensor_f32 weight pre-cache for VITS-family TTS
- L12894 — Single-token embed graph elimination for LLM-ASR
- L13408 — A once-allocated-then-reused bucket step graph is a CUDA-graph-capture hazard: reset+alloc the sched EVERY step
- L13467 — A cached cgraph is invalidated by ANY other graph on its scheduler — reset+alloc per step is NOT enough
- L13567 — TTS code predictor: the dispatch overhead is real, but "fuse into one graph" and "skip the reset" are BOTH wrong — the fix is a sched-free persistent graph
- L13636 — Any T-sized `ggml_view` into a fixed-length weight table must be bounded by the tensor's real length — long inputs overflow it and abort
- L13778 — gallocr does NOT protect input-flagged tensors across computes — re-set EVERY input before EVERY compute of a persistent graph
- L13839 — Vocoder/codec conv graphs hide three graph-construction wastes that dwarf the actual conv FLOPs — profile per-node before assuming it's inherent
- L13886 — Before optimizing a graph's *dispatch*, split host-encode vs GPU-execute — and never trust a GPU benchmark taken under load
- L13981 — A "GPU" model can be silently running on CPU — audit for hot graphs pinned to backend_cpu and CLI adapters that never forward use_gpu
- L17469 — PR #244 dequant-cache fix MERGED

**GPU portability — Metal / CUDA / Vulkan** (24)

- L1154 — When the full system needs an unavailable resource (model / GPU), factor the risky logic into a pure helper and prove IT on synthetic data
- L1269 — Verify a roadmap "broken/OPEN" claim empirically before implementing it — the codebase may have outgrown the note
- L1349 — A Metal masked-attention-padding penalty does NOT transfer to CUDA, and a bucket-width change is byte-identical on Metal/CPU but not on CUDA
- L1396 — ggml-metal im2col starves batch-1 convs — 3–11 threads/threadgroup at N=1, ~40× below bandwidth
- L2058 — A "garbled GPU output" bug can live entirely downstream — A/B the GPUs before localizing (§192)
- L2112 — MoltenVK `mul_mm`/`mul_mat_vec` downconvert src0 to f16 regardless of stored dtype (§192)
- L2123 — Vulkan has no `REPEAT f16→f16` — cast K/V to F32 *before* the GQA repeat (§192)
- L2136 — On macOS, GGML_METAL is default-ON and silently wins over Vulkan (§192)
- L2186 — §214 Metal batched (B=2) quantized mat-vec ≠ the single-token PREC_F32 path
- L2397 — Metal's q8_0 mat-vec kernel requantizes activations to q8 and ignores the F32 prec hint (§205)
- L2895 — Measuring GPU perf on a thermally-throttling laptop GPU
- L9140 — WDDM idle-clock-state hysteresis on consumer/laptop NVIDIA SKUs
- L9277 — Chatterbox #83 Round 9 — S3Gen UNet GPU drift on Metal
- L10530 — FA per-head additive mask CUDA kernel — what the upstream signature already gave us
- L11148 — funasr CUDA !-loop — all-NaN prefill logits (issue #125, §136)
- L12458 — Cohere flash-attn crossover
- L12518 — conv_transpose_1d GPU TDR — naive loop is O(IL), not O(K/s0)
- L12720 — flash_attn_ext vs scalar attention — numerical divergence
- L12831 — VibeVoice TTS garbles only on AMD RDNA4: coopmat2 flash-attention
- L14016 — Micro-optimizing an already-BLAS'd path with a GPU-ggml port is usually a dud — check for existing Accelerate/cblas and measure encode-vs-execute BEFORE porting
- L14339 — "Backend miscomputes my pipeline" ≠ "op X is broken": arbitrate with test-backend-ops, and beware aggregate precision (#304 native-Vulkan post-mortem)
- L16257 — Chatterbox GPU UNet performance fix
- L16496 — cosyvoice3 \"CUDA test FAIL\" was a registry-alias + flow-quant discovery bug, NOT a CUDA bug
- L17787 — "GPU picks a different token than CPU" is usually NOT a miscompute in an AR audio model — dump the LOGITS, and don't reach for the repetition detector

**Quantization** (10)

- L1613 — Auditing CrispASR against CrispEmbed's bug classes: a weight reader with no quantized branch fails silently (2026-07)
- L1693 — Quantizing a forced-aligner: q8 everywhere (incl. lm_head) is bit-identical; q4 on the encoder is not (#192 TADA aligner)
- L2743 — Orpheus/SNAC TTS: `token_embd` must stay F16 for sub-Q8 quants
- L3345 — Quantisation and memory
- L3620 — Quantization
- L12449 — F16 precision loss in deep WaveNet stacks
- L12466 — MeloTTS + BERT quantization — what works and what doesn't
- L13560 — Re-quantized GGUFs on HF must be re-baked when the quantizer adds carve-outs
- L13648 — Sub-8-bit quantization of an audio tower fails as BEHAVIORAL collapse (loops, empty output) — and there is no per-block cliff to bisect
- L17284 — imatrix quant SHIPPED

**Perf measurement & A/B discipline** (8)

- L1287 — Per-step matvec dispatch overhead is LOAD-DEPENDENT — a "win" measured on a busy box is mostly a contention artifact
- L1757 — Best-of-N only helps if the scorer measures what you care about — TADA's reconstruction scorer is blind to duration outliers (#192)
- L3381 — CPU vs ONNX vs PyTorch baselines
- L3824 — Performance: what faster-whisper / insanely-fast-whisper do
- L5121 — 2026-04-23 - FireRed decoder optimization triage
- L10829 — Distinguishing "slow run" from "hung run" — CPU time ≪ wall time is the signal
- L10964 — Cross-backend bug-sweep methodology — pair the cap survey with an empirical A/B
- L12902 — §176 runtime optimization audit methodology

**Long-form audio, VAD & chunking** (8)

- L1518 — LLM-ASR (SALM) instruction-echo on short windows is genuine model behaviour, not a port bug — gate the input, don't chase the prompt
- L1571 — The Mimi codec transformer must be causal — non-causal silently truncates long audio; the >250-frame WER A/B settled it (2026-07)
- L2964 — VAD + chunking
- L3897 — VAD integration and long audio
- L10653 — TDT single-pass over a full long utterance is numerically fragile to codec-level audio noise
- L14761 — #227 "--vad-import wants ggml-tiny" = LID not VAD
- L14983 — #300 streaming diarization SHIPPED
- L15579 — #89 parakeet-ja long-form FIXED

**Tokenizers, prompts, language & text** (17)

- L430 — MOSS-TTS-Local 4B stop runaway was a PROMPT-TOKENIZATION bug — non-compositional BPE, not the forward (#249, 2026-07)
- L480 — Cross-lingual TTS needs the target language plumbed through /v1/audio/speech (#249/#304, 2026-07)
- L1769 — A converter that embeds a tokenizer MUST embed the merges — a silent `try/except: pass` byte-fallback breaks everything downstream (#192)
- L3467 — Language handling
- L5288 — FireRedPunc / fullstop-punc — BERT punctuation restoration
- L6293 — Chatterbox 24 kHz prompt mel — module 4 phase 3
- L7452 — Text LID via fastText — GlotLID-V3 + LID-176
- L7663 — Text LID via CLD3 — Google compact language detector
- L12753 — VibeVoice TTS: missing BPE merges still need BPE behavior
- L13283 — 2026-07 — SentencePiece tokenizer taxonomy: greedy longest-match is wrong for BOTH Unigram and BPE
- L13540 — SentencePiece `byte_fallback` is not optional decoration — OOV emoji/symbols must become `<0xHH>` byte tokens, and a `utf8_aligned` Viterbi dead-ends at a multi-byte lead without it
- L13689 — The prompt contract is part of the port: a specials-only tokenizer silently sent EVERY glm-asr prompt instruction-less
- L13724 — Unigram run-length loop metrics pass 2-gram cycles — and a "raw" baseline is only raw if the disable gate actually exists
- L14897 — #249 MOSS-Local 4B stop-runaway = PROMPT tokenization bug
- L15276 — #329 TTS target language
- L16350 — cohere language whitelist + probe LID
- L16468 — CV3 phase 6 speech_tokenizer_v3 ggml port

**Mel, codecs & the audio front-end** (15)

- L1507 — A one-frame error in a codec ENCODER shifts every RoPE position and shows up as an onset artifact, not a gradual drift — and a padding scheme faithful to one codec can be wrong for another
- L2371 — A glibc-only crash reproduces on macOS under AddressSanitizer — and the bug is often a non-power-of-two FFT (§205)
- L2662 — Streaming conv modules need cached left context, not zero-padding (#81)
- L2840 — FFT size must match upstream exactly (Mini-Omni2 / Whisper mel)
- L3149 — mel / preprocessor
- L3297 — Mel spectrograms
- L3435 — Audio format lessons
- L4406 — Kyutai STT: causal padding, interleaved RoPE, and codec-based ASR
- L4540 — FireRedVAD: FSMN Conv1d replication
- L6341 — Chatterbox atomic native voice clone — the resampler + 5-cond install
- L11318 — moshi / Mimi RVQ codebooks: decode uses embed_sum / cluster_usage
- L12341 — MeloTTS (VITS2) — from zero to BERT conditioning
- L14858 — #245/§232 qwen3-tts CP_DIRECT + codec FASTCONV DONE
- L17183 — glint MP3+AAC TTS output §225 SHIPPED
- L17666 — voxcpm2 CausalTransposeConv1d kwargs gotcha

**Process, triage & documentation discipline** (18)

- L402 — When instrumenting the suspect file produces NO output, you are editing the wrong file — look for a second copy
- L495 — "The model emits it inline as text" is a claim to VERIFY, not to document — structured data you never parsed looks identical to a model limitation
- L752 — A positional arg landing on the WRONG parameter is invisible in review and baked into the weights
- L957 — PLAN "OPEN" items are frequently already shipped — audit against the CODE, never the prose
- L1195 — Audit a whole roadmap CLUSTER in one measure-first pass before implementing any of it
- L1501 — Handover docs can be dangerously stale — read the actual Python source, not the handover
- L2250 — DRY shared headers can exist for months before callers migrate (§175)
- L3543 — Regression testing discipline
- L3570 — Specific bugs that cost us a day each
- L4033 — CLI ↔ library DRY refactor
- L5597 — Audit script ≠ behavior test
- L10738 — Long-form ASR has three distinct failure classes, not one (2026-05-25, generalising issue #89)
- L12664 — beam_size default — greedy vs beam-5
- L12788 — VibeVoice TTS start clicks: distinguish decoder PCM from CLI post-processing
- L13336 — Issue #89 close-out: four transferable lessons
- L13631 — Issue triage discipline: check the codebase before leaving issues open
- L14258 — `git apply --3way` STAGES its result — a later `git add X && git commit` sweeps it up
- L16536 — CrispEmbed #31 WASM OCR e2e

**Per-model port notes** (44)

- L250 — A model's capability list cannot be inferred from its vocabulary — and forcing a fake list does not simulate having the capability
- L2855 — Multi-stream token architecture (Mini-Omni2)
- L3505 — Model architecture comparisons
- L4449 — FireRedASR: Conformer encoder debugging
- L5402 — TTS / Vocoder (Chatterbox HiFTGenerator)
- L5664 — Chatterbox-Turbo conformer encoder — ggml layout traps
- L5715 — Chatterbox repaired GGUF split — stage-specific regen
- L5890 — Chatterbox voice cloning — bake to GGUF, load via `--voice`
- L5943 — Chatterbox VoiceEncoder native port — module 2 of voice cloning
- L6049 — Chatterbox S3Tokenizer V2 native port — module 3 of voice cloning
- L6218 — Chatterbox CAMPPlus phase 2 — TDNN forward
- L7114 — T5-family translation runtime traps (May 2026, MADLAD-400 debugging)
- L7820 — IndexTTS-1.5 TTS backend
- L8287 — Speaker verification — TitaNet
- L8335 — Parakeet-TDT greedy decode — blank + duration=0
- L8401 — VibeVoice 1.5B TTS voice cloning: acoustic + semantic dual encoder
- L8884 — SANM-encoder family (FunASR / SenseVoice / CosyVoice)
- L11277 — Round 10 — SpeechT5 + Dia TTS backend ports (2026-05-31/06-01)
- L11352 — Dia 1.6B TTS — what ACTUALLY fixed it
- L11406 — FastPitch TTS — non-autoregressive parallel TTS port (§133, 2026-06-02/03)
- L11572 — SpeechT5 TTS decoder — what ACTUALLY fixed it
- L11646 — Parler TTS — T5 + MusicGen decoder + DAC 44 kHz
- L11778 — MAES beam search for TDT transducers
- L11880 — Bark TTS — what ACTUALLY fixed it
- L12427 — OpenVoice2 voice cloning — ggml data layout
- L13165 — TADA encoder port: staged model loading on constrained RAM (§221)
- L14479 — #195 parakeet-ctc-1.1b-ja CTC routing
- L14522 — #205 --max-len text-split + granite-plus
- L14639 — #221 irodori voice cloning SHIPPED
- L14802 — #231 cohere-arabic loop = corrupt GGUF
- L14915 — #249 MOSS voice cloning SHIPPED
- L14963 — vibevoice #299 runtime-reproduced
- L15028 — #308 whisper subtitles double-caps
- L15509 — #334 cosyvoice3 clone bugs
- L15647 — #93 voxtral-tts SHIPPED
- L16127 — ARK-ASR-3B port SHIPPED
- L16631 — CSM-1B TTS §135 FIXED
- L16675 — Dia 1.6B TTS port
- L16851 — dots.tts #200 SHIPPED
- L17224 — higgs-audio-v3-stt SHIPPED
- L17355 — indextts long-ref crash FIXED
- L17594 — TADA TTS time embedding bugs
- L17642 — Four bugs that together caused empty/garbage transcripts; all patched in src/vibevoice.cpp +…
- L17867 — "Verified byte-identical at 225 s" verified a clip, not a length — and a decoder that drops spans needs a repair pass, not a better cap

## Cross-reference by model / family

A section is listed when its heading names the family, or its body mentions it
at least 3 times. Sections appear under several families; this is a
lookup, not a partition.

- **chatterbox** (26) — L2186, L2371, L5402, L5664, L5715, L5747, L5786, L5890, L5943, L6049, L6152, L6218, L6293, L6341, L8490, L9277, L12902, L13950, L14339, L14399, L14858, L15647, L16257, L16295, L16851, L17925
- **cosyvoice3** (9) — L570, L8884, L12902, L13950, L14399, L15276, L15509, L16468, L16496
- **vibevoice** (14) — L495, L1646, L2553, L5121, L5890, L8401, L12753, L12788, L12831, L13507, L14963, L14983, L15214, L17642
- **tada** (9) — L976, L1349, L1693, L1743, L1757, L12902, L13165, L15891, L17594
- **whisper** (22) — L170, L1862, L2840, L3640, L3824, L3897, L4033, L4540, L5481, L7452, L7820, L12664, L14522, L15028, L15071, L15137, L15579, L16127, L16468, L17224, L17284, L17400
- **parakeet / TDT** (17) — L703, L1862, L1895, L2964, L3897, L4243, L6341, L8335, L10653, L10738, L11778, L13336, L14479, L14522, L15579, L17400, L17867
- **moss** (13) — L430, L626, L1118, L1320, L1495, L1501, L1792, L1817, L1842, L2964, L14142, L14897, L14915
- **firered** (9) — L1287, L4449, L4540, L5121, L5288, L6152, L13283, L15028, L15071
- **funasr / sensevoice / SANM** (2) — L8884, L11148
- **kyutai / moshi / mimi** (7) — L1507, L1571, L4406, L11318, L12092, L12248, L16631
- **dia** (5) — L1249, L11277, L11352, L11572, L16675
- **parler** (1) — L11646
- **speecht5** (2) — L11277, L11572
- **bark** (1) — L11880
- **pocket-tts** (3) — L12092, L12248, L17469
- **melotts / VITS** (5) — L1413, L12341, L12466, L12881, L15949
- **fastpitch** (1) — L11406
- **indextts** (1) — L7820
- **orpheus / SNAC** (1) — L2743
- **voxcpm2** (4) — L8490, L8694, L8817, L17666
- **openvoice2** (1) — L12427
- **glm-asr** (1) — L13689
- **qwen3** (17) — L1320, L3194, L3897, L4540, L7219, L7820, L8490, L8884, L10866, L13648, L13746, L13839, L14522, L14858, L15276, L17284, L17787
- **madlad / T5** (1) — L7114
- **mini-omni2** (2) — L2840, L2855
- **titanet / speaker-id** (1) — L8287
- **audioseal** (1) — L12487
- **cohere** (7) — L250, L3297, L10802, L12458, L14522, L14802, L16350
- **voxtral** (11) — L3194, L3297, L3345, L3505, L3897, L10738, L10802, L10829, L10964, L15647, L16851
- **granite** (3) — L1937, L3194, L14522
- **ark-asr** (2) — L16127, L16851
- **higgs-stt** (2) — L626, L17224
- **dots-tts** (2) — L1501, L16851
- **irodori** (2) — L1378, L14639
- **f5-tts** (1) — L13981
- **kokoro** (4) — L343, L7219, L15276, L15949
- **zonos** (1) — L11646
- **omniasr / omnivoice** (6) — L170, L1073, L4540, L13778, L13806, L14192
- **glm / mimo / mega** (3) — L10994, L13689, L13746
